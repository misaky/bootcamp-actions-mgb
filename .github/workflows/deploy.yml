name: Deploy to server

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    env:
      NAME: Codigofacilito
    
    steps:
      - name: Hola Mundo
        run: echo "hola mundo"

  second_job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first_job
    runs-on: ubuntu-latest
    
    steps:
      - name: Hola Mundo
        run: echo "hola mundo segundo"
